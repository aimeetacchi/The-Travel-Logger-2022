import { Box } from '@mui/material';
import styled from "styled-components";


const PlacesStyles = styled(Box)`
    width: 100%;

    .container {
        width: 100%;
        margin: 20px auto;      
    }

`

export default PlacesStyles;